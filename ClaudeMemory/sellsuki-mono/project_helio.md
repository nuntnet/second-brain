---
name: project-helio
description: "Helio — Control Tower hyperautomation + EPM platform; architecture, component status, and Phase 0 blocker"
metadata: 
  node_type: memory
  type: project
  originSessionId: fa60f183-a714-4e6c-bbea-d832eec8343c
---

**Helio** = group-wide AI operations platform per blueprint at `/Users/nunt/helio-project/control-tower-hyperautomation-epm-blueprint.md`. 4-layer arch, all systems talk via MCP, multi-tenant (8 entities: Kruengkao Group companies + Sellsuki), strict per-entity isolation. Iron rule: AI never executes finance — always human approval gate in n8n.

**Component status (as of 2026-06-11):**
- Control Plane = **n8n2** (`n8n2.ch-erawan.com`, container on NAS DS1, MCP already connected to Claude Code) ✅
- Brain = **Hermes** (runs on Mac, has multi-tenant memory isolation via `X-Hermes-Session-Key`, OpenAI-compatible API at 127.0.0.1:8642) ✅ but NOT yet reachable from n8n2 — see [[reference-nas-ds1]]
- Specialists = **CrewAI** — not installed (Phase 3, later)
- Observability = **Paperclip** — optional (Phase 5)
- Task system-of-record = Notion "Secretary — Task Hub" (Notion MCP available)
- Financial actuals = Peak Engine; plans/valuation = Excel models

**Hermes↔n8n bridge — SOLVED via Option B (2026-06-12):** Hermes now runs as a dedicated api-server-only Docker container `hermes` ON THE NAS, on network `tunnel-net` (same as n8n2). n8n2 reaches it at `http://hermes:8642/v1` — no tailscale, no cross-machine hop. Verified end-to-end: n8n2 → hermes → Gemini → response (`HELIO_OK`). Build scaffold at `/Users/nunt/helio-project/hermes-nas/` (Dockerfile, config.yaml, .env, README). Source staged from Mac's `~/.hermes/hermes-agent` (excl. venv/web/ui-tui/node_modules/sessions/skills), built on NAS as `hermes-nas:latest`, runtime config bind-mounted from `/volume1/docker/hermes-nas/data`. NAS API_SERVER_KEY = hermes_adb8332717b10b60fdc155fa573ccff6b27b2f8df9699e38. Provider = Gemini (model gemini-2.5-flash; gemini-2.5-pro hit 429 quota-exhausted on the free-tier key). Per-tenant isolation: n8n sends `X-Hermes-Session-Key: <entity-id>` per call. **Planned: switch provider to OpenAI Codex (openai-codex OAuth, ChatGPT subscription) later — runtime config change, no rebuild; do `hermes auth` codex login interactively via docker exec, tokens persist in /data/auth.json.** hermes-agent supports openai-codex provider (OAuth via auth.openai.com → chatgpt.com/backend-api/codex), analogous to the Mac's Claude adapter.

**Bidirectional brain↔control-plane DONE (2026-06-12):**
- hermes→n8n MCP wired & verified: NAS hermes has the n8n MCP as a tool (29 tools, `mcp_n8n_*`), config in /data/config.yaml `mcp_servers.n8n` → `http://n8n2:5678/mcp-server/http` with `Authorization: Bearer ${N8N_MCP_TOKEN}` (token in /data/.env). GOTCHA: the python `mcp` SDK is in hermes' [dev] extra NOT core — Dockerfile needs a dedicated `RUN pip install "mcp>=1.2.0,<2"` layer or MCP is silently disabled (discovers 0 tools).
- **Notion Task Hub created** in a NEW dedicated Notion workspace "Helio" under cherawansystem@gmail.com (separate from ch-erawan-next website ws). DB "🤖 Secretary — Task Hub" id `37cb3d71-aa1b-818d-96f2-c4cb6cc8f6b0` (https://app.notion.com/p/37cb3d71aa1b818d96f2c4cb6cc8f6b0), parent page id `37cb3d71-aa1b-8059-a2cd-e768f6631630`. Schema: Task, Owner (Accountable, people), Entity (9 select), Status, Priority, Due, Effort (h), Source, Escalation, OKR (text), Blocked by/Blocks (self-relation dependency). Access via Notion **internal integration token** `ntn_48275499...` (scoped to Helio ws). Notion is reached THROUGH n8n (blueprint: n8n = sole orchestrator), NOT as a hermes MCP.

**Phase 0 workflow DESIGNED + data layer built (2026-06-12):** Design in `helio-project/docs/05-phase0-workflow-design.md` (+ a flow/timeline diagram was rendered). Three n8n workflows: `helio-task-assigned` (Notion trigger→LINE), `helio-reminder-escalation` (daily cron: classify due/overdue, group by owner, Hermes composes one anti-spam message per owner via session X-Hermes-Session-Key=owner:<id>, LINE; overdue≥3d→L1 Slack team lead, ≥7d→L2 email entity exec + Notion update), `helio-monday-digest` (weekly exec digest). Key principle: n8n gathers data, Hermes composes/dedupes, LINE delivers. Notion accessed THROUGH n8n (not hermes directly).
- ✅ Built in Notion Helio ws: Task Hub DB `37cb3d71-aa1b-818d-96f2-c4cb6cc8f6b0` + Directory DB `37cb3d71-aa1b-81a6-9441-e4446ad2c078` (routing: Name/Notion Person/Entity/Role/LINE User ID/Slack ID/Email). Parent Helio page `37cb3d71-aa1b-8059-a2cd-e768f6631630`.
- ✅ Workflow v2 (WORKING design) id `l1wSw3s10FtdldW6` (https://n8n2.ch-erawan.com/workflow/l1wSw3s10FtdldW6). v1 (`cUBOOmYD5bVy3bDU`) ARCHIVED — it had 2 bugs found on first run: (1) Notion simple-mode output keys are `property_task`/`property_due.start`/`property_status`/`property_entity` (prefixed `property_`), not the display names; (2) the n8n AI **Agent** node (ToolsAgent v3) crashes with hermes — "Cannot read properties of undefined (reading 'map')" (expects tool-calling response shape). FIX in v2: call hermes via plain **HTTP Request → http://hermes:8642/v1/chat/completions** (proven), header-auth cred; Code node reads property_* keys + returns [] when no tasks (skip send). v2 nodes: scheduleTrigger → Notion getAll (cred auto-assigned "Notion account") → Code classify → HTTP "Hermes compose" (httpHeaderAuth "Hermes — Bearer" = Authorization: Bearer <NAS key>) → HTTP "LINE push" (httpHeaderAuth + body REPLACE_WITH_LINE_USER_ID). 2 test tasks seeded in Task Hub (due-soon + overdue). Lesson: don't use the n8n AI Agent node with hermes — use HTTP to /v1/chat/completions.
- (archived) v1 id `cUBOOmYD5bVy3bDU`
- **LOOP PROVEN END-TO-END (2026-06-12)** via v2 manual runs: Notion getAll → classify (found the 2 seeded tasks) → Hermes(HTTP /v1/chat/completions) composed a clean Thai consolidated reminder → LINE push auth OK. Credential gotchas hit + solved: (1) Header Auth — Name field must be `Authorization` (not the credential's display name → "must be a valid HTTP token"); (2) Hermes & LINE must use SEPARATE Header Auth credentials (sharing one swaps the token). Final blocker: LINE push 400 "You can't send messages to yourself" — the `to` userId used was the OA's OWN id. Need a real human userId (who added the OA). Built capture webhook: workflow `ZHd54cWR8Nzqj4lZ` (ACTIVE) at `https://n8n2.ch-erawan.com/webhook/line-capture` → extracts events[].source.userId. User must set this as the OA's webhook URL, add OA, message it → read execution for the real userId → put in v2 LINE node `to`. Hermes credit is FINE (no 429; gemini-2.5-flash working).
- ✅✅ **PHASE 0 LOOP FULLY WORKING (2026-06-12)** — real LINE message delivered to the user's personal LINE. OA = @ykg2018o "ทดลอง" (own userId Ud4b1087b... = the "send to yourself" trap). User's real LINE userId = `U5432d9b2b5211a6af7e24ccd31f93a49` (captured via the line-capture webhook). v2 workflow `l1wSw3s10FtdldW6` now sends a consolidated Thai reminder end-to-end. PUBLISHED/ACTIVE (2026-06-12) — runs daily 09:00 ICT (cron 0 9 * * *), sends to userId U5432d9b2b5211a6af7e24ccd31f93a49. Hermes prompt refined (Thai, no personal name, urgent-first). Capture webhook `ZHd54cWR8Nzqj4lZ` still active at /webhook/line-capture. n8n MCP tools now load natively in Claude (mcp__n8n__*) — no more curl helper needed. NOTE: 2 test tasks ("ทดสอบ...") still in Task Hub → will trigger a reminder every 09:00 until deleted. PILOT entity filter currently '' (all). Phase 1 next: per-owner routing via Directory + L1/L2 escalation.
- **MIGRATED to ds3/n8n3 (2026-06-12):** Second NAS **DS3** — reachable directly via LAN `ssh -p 2022 nunt@192.168.4.10` (key installed; also tailscale 100.120.167.89; domain ds3.ch-erawan.com = Cloudflare). DS3 specs: x86_64, 4 cores, 11GB RAM, Docker, NOPASSWD docker set. n8n3 = `n8nio/n8n` on DS3 network **tunnel-net**, web https://n8n3.ch-erawan.com, MCP token (JWT sub e1bbe839...) iat 1781274351. hermes redeployed on DS3 (container `hermes` on tunnel-net, image hermes-nas:latest built on ds3, /data at /volume1/docker/hermes-nas/data; config n8n url → http://n8n3:5678/mcp-server/http; same API_SERVER_KEY hermes_adb83...; GOOGLE_API_KEY = AQ.Ab8RN6KYV8jdHbh...; reuse). Verified: n8n3↔hermes 2-way (Gemini "DS3_OK", 30 MCP tools). Reminder Engine recreated on n8n3 = id `1EPv4kgW0dxOHbV8` (unpublished) with real LINE userId U5432... baked in + refined no-name Thai prompt. NEXT: user attaches 3 creds on n8n3 (Notion ntn_48275499..., Hermes header-auth Bearer hermes_adb83..., LINE header-auth Bearer <line token>), then test+publish. ⚠️ n8n2 (ds1) reminder engine `l1wSw3s10FtdldW6` STILL PUBLISHED/active → must unpublish to avoid DOUBLE daily LINE reminders once n8n3 is published. n8n2 reachable via curl helper /tmp/n8ncall.sh (n8n2 token). n8n3 helper /tmp/n8n3.sh (note: its parser prints EMPTY but the call succeeds — use direct curl to read results).
- **ds1 DECOMMISSIONED (2026-06-12):** user done with n8n2. n8n2 MCP token went DEAD (401) so couldn't unpublish via MCP → stopped the ds1 containers instead: `docker stop n8n2 hermes` on ds1 (both Exited now). No more duplicate reminders. Reversible (docker start); data still on ds1 disk. helio now lives entirely on **ds3**.
- ds3 reminder engine `1EPv4kgW0dxOHbV8` VERIFIED working (manual Execute → LINE delivered a clean Thai consolidated reminder, refined prompt = "เรียนทุกท่าน", no name). STILL UNPUBLISHED (manual only) — publish to auto-run daily 09:00.
- **PHASE 0 COMPLETE + LIVE (2026-06-12):** n8n3 reminder engine `1EPv4kgW0dxOHbV8` PUBLISHED/active on ds3 → auto daily 09:00 → LINE U5432... helio fully on ds3 (ds1 stopped). Vision+full roadmap doc written: `helio-project/docs/07-vision-and-roadmap.md` (north star = "one spine, two loops": Control Tower inward + Growth/Marketing engine outward, joined by a flywheel; borrows Oracle/BeehiveAI patterns — knowledge graph, multi-agent, retrospective loop, voice, dashboard). Phase 1 next (build-ready, needs DATA from user): per-owner routing (Directory rows + task owners), dependency nudges (Blocks field), L1/L2 escalation (needs team-lead/exec contacts + Slack/email), delivery dashboard. NOTE: 2 test tasks still in Task Hub → will trigger daily 09:00 reminder until deleted.
- **PHASE 2 EPM — OKR cascade BUILT (2026-06-12):** 4-level cascade as linked Notion DBs in Helio ws (doc `helio-project/docs/08-epm-okr-cascade.md`). DBs: 🎯 Strategy `37db3d71-aa1b-8140-975e-c63b2ee5f8ee` ← 📊 Objectives/EntityOKR `37db3d71-aa1b-8113-a0d8-edd9de1a75e2` ← 📈 Key Results & KPIs `37db3d71-aa1b-8177-bf7d-ccac2169b55d` ← 🤖 Task Hub (added relation "Contributes to (KR/KPI)"). KR DB has Target/Actual/Actual Source + formula Variance (Actual-Target) + Attainment % — VERIFIED (sample MRR 1M/650k → Variance -350k, 65%). Sample cascade created (Strategy "Validate Sellsuki PMF" → Objective → MRR KR → linked test task). NEXT Phase-2: n8n Peak Engine connector (pull actuals → KR.Actual), variance-trigger workflow (breach → auto investigation task w/ Hermes context), monthly EPM digest, cascade-completeness check. Notion person id (user) = 35dd872b-594c-8165-ac41-0002e457fa2a.
- **Frontend v1 = Notion Control Tower page** (2026-06-12): https://app.notion.com/p/Helio-Control-Tower-37db3d71aa1b81669809c56f2064f6cd — group snapshot + risk board + link_to_page into all 5 DBs (live). Snapshot is point-in-time (refresh via n8n later); links always live. Notion API can't create chart/linked-view configs → user adds charts in UI. Real custom dashboard (read Notion API → render, container on ds3 behind tunnel → helio.ch-erawan.com) = Phase 5, not built. Dashboard mockup was shown in chat. **PHASE 2 EPM LOOP CLOSED + LIVE (2026-06-12):** Variance-trigger rebuilt with n8n Notion nodes (reuses existing "Notion account" cred — auto-assigned, no manual cred step) = workflow `UsJ67ulfpHCZ0H3P` PUBLISHED, daily 08:00. HTTP version `9CsTgqG5y3kqqaiN` archived. VERIFIED end-to-end: detected MRR breach (65% <80%) → auto-created investigation task "⚠ ตรวจสอบ variance: MRR (65%)" in Task Hub (Source=Escalation, linked to KR) + flagged KR (Status=Breach, Investigation Open=true → dedup, next run skips). EPM "variance as trigger" working. Notion node create/update param format: propertyValues[{key:"PropName|type", type, title/selectValue/relationValue/checkboxValue}]; pageId/databaseId = {__rl:true,mode:"id",value}. Lesson: rebuild Notion-write workflows with n8n Notion node (not HTTP) → auto-assigns existing notionApi cred, zero manual cred step.
- **Monthly EPM digest BUILT (2026-06-12):** workflow `uMoDI0y1jrCG14jo` on n8n3, cron monthly 1st 09:00. Flow: Notion getAll KRs + getAll Objectives (executeOnce on 2nd to avoid item-multiplication) → Code joins KR→Objective→Entity, rolls up avg attainment + breach count per entity → HTTP Hermes /v1/chat/completions composes Thai exec digest → HTTP LINE push to exec U5432.... Notion nodes auto-cred; 2 HTTP nodes (Hermes EPM digest, LINE push exec) need user to RE-SELECT existing header-auth creds "Hermes — Bearer" + "LINE OA — Bearer" from dropdown (they exist on n8n3), then test+publish. n8n workflows on n8n3 now: reminder (1EPv4kgW0dxOHbV8, live), variance-trigger (UsJ67ulfpHCZ0H3P, live), EPM digest (uMoDI0y1jrCG14jo, pending cred+publish).
- **PHASE 3 CrewAI specialists BUILT + LIVE (2026-06-12):** CrewAI MCP server (FastMCP, Streamable HTTP) as container `crewai` on ds3 tunnel-net, reachable `http://crewai:8810/mcp`. Build context `helio-project/crewai-nas/` (server.py + requirements [crewai,mcp,litellm] + Dockerfile + .env with GEMINI_API_KEY=AQ.Ab8... CREW_MODEL=gemini/gemini-2.5-flash). 2 tools VERIFIED working: `variance_deep_dive` (Analyst→Advisor crew) + `valuation_review` (Valuer→Reviewer crew) — ran variance_deep_dive on MRR 65% → real Thai 3-action analysis. Wired into Hermes: `mcp_servers.crewai` added to /data/config.yaml (ds3 hermes config now has n8n + crewai), restarted, Hermes discovers 36 tools incl. the 2 crewai. Doc `helio-project/docs/09-crewai-specialists.md`. CrewAI = stateless TOOL; Hermes (brain) decides when to call it. To add crews: edit server.py @mcp.tool()+Crew, rebuild, recreate, restart hermes. Tested crewai via mcp client SDK inside hermes container (proper initialize/session). 4 containers on ds3 now: n8n3, hermes, crewai, cloudflare-tunnel.
- **CrewAI WIRED INTO AUTOMATED LOOP (2026-06-12):** variance-trigger upgraded to v3 `MXb5OVDtGRx9QVE4` (PUBLISHED, daily 08:00; v2 UsJ67ulfpHCZ0H3P archived). Added CrewAI HTTP step: CrewAI service now also exposes plain HTTP routes via FastMCP `@mcp.custom_route` (`/health`, `/run/variance_deep_dive`, `/run/valuation_review`) alongside MCP — so n8n calls it directly (no MCP handshake, no auth, internal). Flow: detect breach → HTTP POST http://crewai:8810/run/variance_deep_dive → Notion create task with the deep crew analysis in new "Investigation" rich_text field (truncated 1900 chars) → flag KR. VERIFIED: MRR 65% → task created with real CrewAI churn deep-dive analysis + KR flagged Breach/InvOpen. Task Hub gained "Investigation" rich_text property. Lesson: n8n→CrewAI direct via custom_route HTTP is deterministic (crew always runs) vs routing through Hermes (LLM may not call tool).
- **PHASE 5 dashboard (frontend v2) BUILT + RUNNING (2026-06-12):** `helio-dashboard` container on ds3 tunnel-net — Flask (gunicorn) server-side reads Notion (OKR cascade KRs + Objectives + Task Hub) → renders live control tower (metric cards, entity scorecards rolled up by KR→Objective→Entity, KR table w/ attainment bars, risk board). Build context `helio-project/dashboard/` (app.py, requirements, Dockerfile, .env w/ NOTION_TOKEN). Image helio-dashboard:latest. VERIFIED render internally (shows live MRR data). Running INTERNAL-ONLY (no -p) — auto-mode guard blocked host-port exposure of the unauth'd token-bearing service. To VIEW: user runs recreate-with `-p 8899:8080` (LAN http://192.168.4.10:8899 / tailscale http://100.120.167.89:8899) OR expose via cloudflare tunnel + Cloudflare Access (helio.ch-erawan.com). Dashboard has NO built-in auth → LAN/tailscale ok, public needs Access. 5 containers on ds3: n8n3, hermes, crewai, helio-dashboard, cloudflare-tunnel.
- **GROWTH ENGINE pillar OPENED (2026-06-12):** added 2 crews to the crewai service → 4 tools total: variance_deep_dive, valuation_review, **market_research** (Researcher→Strategist), **copywriting** (Copywriter→Editor/brand-safety). Each is an MCP tool (Hermes) + HTTP route (/run/market_research, /run/copywriting) for n8n. VERIFIED: market_research on Bridge Records → real Thai growth strategy (Vibe Music wedge, TikTok/Reels). server.py updated, rebuilt, recreated crewai, restarted hermes. Pillar-2 intelligence core live; next for Growth Engine: channel orchestration (n8n publish to LINE OA/social), the flywheel (helio detects revenue-OKR miss → propose campaign → human approve → execute → results→scorecard), and optionally wire BeehiveAI (~/BeehiveAI, marketing multi-agent w/ Queen/Worker/Drone/Guard + pp/xx/rr skills) as the campaign exec engine.
- STILL QUEUED (roadmap): Phase 4 (8-entity scale — workflows already entity-agnostic PILOT=''; consolidation roll-up; Business Knowledge Graph Chroma+DuckDB as new ds3 container); Growth Engine pillar (market_research+copywriting crews); Peak Engine connector (auto actuals); loose ends — EPM digest publish (needs user to attach Hermes-Bearer + LINE-Bearer creds via dropdown on uMoDI0y1jrCG14jo), Phase 1 per-owner routing+escalation (needs Directory team rows + LINE userIds + escalation contacts).
- **CrewAI telemetry blocks 20s/call (FIXED 2026-06-18):** CrewAI's tracing prompt ("Would you like to view your execution traces? [y/N] (20s timeout)") blocks 20s on every `/run/<tool>` call inside the container → n8n HTTP node timed out before crewai's 200 returned (the variance-trigger CrewAI node "didn't work"). Fix: recreate `crewai` with `-e CREWAI_TRACING_ENABLED=false -e CREWAI_DISABLE_TELEMETRY=true -e OTEL_SDK_DISABLED=true -e CREWAI_TELEMETRY_OPT_OUT=true` (added to the docker run, alongside --env-file). After fix crew responds ~28s (was ~50s+). n8n CrewAI node also needs: Timeout=180000ms + Retry On Fail (3 tries, 5s wait) to absorb crew latency + transient Gemini 503 ("VertexAIException 503 model experiencing high demand" — transient, retry succeeds). PERMANENT (2026-06-18): these 4 flags are now baked into `crewai-nas/Dockerfile` as `ENV` — rebuilt + recreated WITHOUT -e and verified (TRACING=false in container, crew responds ~30s). Any future recreate/rebuild keeps the fix.
- **LLM-provider decision (2026-06-18): helio NAS stays on Gemini API key, NOT Claude subscription.** User asked whether to run NAS hermes/crewai on a Claude subscription (claude adapter, like the Mac) instead of the Gemini API key. Decided NO: helio is a 24/7 cross-tenant automation workload; Claude subscription (Pro/Max) is built for interactive use, has usage limits (Anthropic moved to weekly limits) + ToS concerns on programmatic use → API key / Gemini is steadier. claude adapter belongs only on the Mac (personal, interactive). Don't re-propose switching helio to Claude subscription unless the user raises it.
- Data persistence: both ds3 containers use bind-mounts (n8n3 → /volume1/docker/n8n3/data:/root/.n8n ; hermes → /volume1/docker/hermes-nas/data:/data) → recreate/remount preserves data; only lost if the host dir is deleted. Next options: refine Hermes prompt (it addressed "Helio" since owner empty), set PILOT entity filter, publish to run daily 09:00, per-owner routing via Directory, add L1/L2 escalation, clean up 2 test tasks in Task Hub. (https://n8n2.ch-erawan.com/workflow/cUBOOmYD5bVy3bDU). 6 nodes: scheduleTrigger(cron 0 9 * * *) → Notion getAll Task Hub (cred "Notion — Helio") → Code classify due/overdue (PILOT entity var, default '') → AI Agent + lmChatOpenAi model=hermes-agent (cred "Hermes (NAS)") → HTTP Request LINE push (httpHeaderAuth cred "LINE OA — Bearer", body has "REPLACE_WITH_LINE_USER_ID"). Built via n8n MCP create_workflow_from_code (curl JSON-RPC; helpers /tmp/n8ncall.sh + /tmp/wf.js source). User must attach 3 creds in UI + set recipient userId + optional PILOT entity, then test/publish.
- ⏳ Earlier note (now partly done) — gated on (a) 3 n8n credentials created in UI: Notion (token ntn_48275499...), OpenAI→hermes (http://hermes:8642/v1 + key, model hermes-agent), LINE (channel access token); (b) pilot entity chosen; (c) ≥1 owner LINE userId seeded in Directory. Also n8n MCP tools not loaded in Claude session (added mid-session → restart to drive natively, or use JSON-RPC tools/call via curl; helper at /tmp/n8nmcp.sh — single-call, no session needed; n8n MCP is effectively stateless for tools/call).

---
**Superseded — earlier failed bridge attempt (Mac hermes over tailscale, 2026-06-11):** kept for context. Mac hermes (personal agent) gateway plist had `API_SERVER_HOST=127.0.0.1` + `API_SERVER_KEY` added during this attempt — this BROKE the Mac hermes-ui/adapter (api_server 8642 → 401 "Invalid API key" to keyless callers). **FIXED 2026-06-12:** removed both vars from ~/Library/LaunchAgents/ai.hermes.gateway.plist via PlistBuddy Delete + bootout/bootstrap (bootstrap needs a retry on I/O error 5) → 8642 keyless 200 restored. NOTE: the plist is NOT in the ~/.hermes git repo (~/.hermes auto-commits config.yaml etc.; plist lives in ~/Library/LaunchAgents). Also changed ~/.hermes/config.yaml compression.threshold 0.5→0.25 (benign, reduces compression warning; auto-committed). Mac tailscale serve was reset (removed). Why tailscale failed:
- Hermes API: bind `127.0.0.1:8642` + `API_SERVER_KEY` (in gateway launchd plist EnvironmentVariables). Key = hermes_2d4b9cce2fbfc72d0327e3bec575b606bdb85406e9f6fcee. Reload gateway via `launchctl bootout` then `bootstrap` (NOT kickstart — kickstart doesn't re-read plist env; bootstrap may need a retry on I/O error 5).
- Exposure: `tailscale serve --bg --http=8642 http://127.0.0.1:8642` (plain HTTP, tailnet-only). NOT direct bind to tailnet IP, NOT serve --tcp: the Mac runs Tailscale **GUI app = userspace networking** → inbound only delivered via `tailscale serve`, not to self-bound listeners. Account has no TLS certs so HTTPS serve/serve --tcp (TLS-passthrough) don't work. Reach via magicdns name `macbook-air--nuntwit.tailf123c9.ts.net:8642` (serve routes by Host header — IP gives 404).
- **NAS-side blocker:** NAS has `tun1000` (tailscale kernel iface) but OS is MISSING the route for the CGNAT range → `ip route get 100.86.220.6` falls back to LAN router → blackhole. `tailscale ping/nc` work (userspace dialer) but curl/n8n (normal sockets) time out. Fix (needs sudo on NAS): `sudo ip route add 100.64.0.0/10 dev tun1000` + add `100.86.220.6 macbook-air--nuntwit.tailf123c9.ts.net` to NAS /etc/hosts (MagicDNS broken on NAS). Route won't persist reboot. n8n2 container also needs the magicdns in its /etc/hosts (extra_hosts) since containers have separate hosts files.
- Note: tenant isolation REQUIRES the API key (hermes memory scoping via X-Hermes-Session-Key needs API-key auth).

**Open blueprint decisions (unanswered):** pilot entity, LINE OA readiness, OKR format, finance approvers. User dismissed these on 2026-06-11 — wait for instruction before building.

- **LEADING-INDICATOR vision (user steer, 2026-06-18):** User explicitly does NOT want helio to just track lagging indicators (monthly revenue/profit historicals = "can't act, too late"). helio's real value = track LEADING indicators (daily: appointment rate/อัตรานัดหมาย, retention, bookings) that FORECAST the lagging outcomes → alert BEFORE month closes → act while there's still time ("predictive control tower, not autopsy"). Design direction for helio v2: (1) tag KRs Leading/Lagging, (2) daily ingest loop for leading metrics, (3) run-rate/AI forecast of the linked lagging KR, (4) predictive alert + CrewAI corrective action while actionable. In the Ford data, อัตรานัดหมาย+Retention (leading) → RO/SA + รายได้ศูนย์ (lagging).
- **FIRST REAL PILOT live (2026-06-18):** Ford Ch-Erawan AfterSale 2026 — imported from user's Excel "ตาราง MPR ฝ่ายบริการ 2569 Ford" (AfterSale Monthly Performance Review; Forecast=Target, Actual=Actual, monthly cols, actuals thru May 2026). Objective `383b3d71-aa1b-81e5-b1c3-cd40b7c713f9` [Entity: Ch-Erawan] + 9 headline KRs (May 2026). Variance loop verified end-to-end (exec 20 success): flagged Active UIO 68% + RO ต่อ SA 78% as Breach, wrote real Thai CrewAI deep-dives to Task Hub. Excel source is OneDrive (cherawansystem) — anonymous/API download BLOCKED (401 unauthenticated); user must download locally + give path, or paste numbers. Cleanup pending: old test KR "MRR" + blank KR row + duplicate MRR investigation tasks.
- helio data input model: Notion IS the DB (no SQL). KR DB has Target/Actual (number, manual-typed) + Variance/Attainment (formula) + Status/Investigation Open (loop-written) + Actual Source (Manual/Financial Model/Peak Engine). 3 input tiers: manual-in-Notion / API-bulk (I create) / automated-ingestion-workflow (later, e.g. Excel→Notion or Peak Engine connector). Objectives DB = the "O" per Entity; KR→Objective→Entity is how the loop+dashboard derive which entity a KR belongs to (KR has no direct Entity field).

- **Leading layer build progress (2026-06-18):** (1) KR DB gained Indicator Type(Leading/Lagging)/Cadence/Forecast EOM/Projected % props; Ford นัดหมาย+Retention tagged Leading/Daily. (2) New **Daily Metrics Log** DB `383b3d71-aa1b-81fc-941f-e11144baf3af` (Reading/Date/Value/KR-relation/Entity/Source/Note) = timeseries store. (3) **Chat UI ingest** added to helio-dashboard app (route /chat + POST /api/ingest): NL text → Hermes parse (HERMES_URL/HERMES_KEY in dashboard .env, calls /v1/chat/completions, session-key helio-ingest-parser, strict-JSON prompt + fuzzy match to KR names) → writes readings to Metric Log. VERIFIED: "Ford อัตรานัดหมายวันนี้ 90, retention 56" → 2 rows written. User picked Chat UI as first of 4 ingest channels (file/chat/LINE/email all to land in same Metric Log core). Dashboard still internal-only (run -p 8899 to access /chat + /). NEXT: forecast loop (read Metric Log → run-rate EOM → write Forecast EOM/Projected % → predictive alert + CrewAI action). hermes-agent reliably did NL→JSON extraction for ingest.

- **helio UI design system (chosen via ui-ux-pro-max, 2026-06-18):** Dark OLED control-tower. Tokens: bg #020617, surface #0F172A / #1E293B, border #334155, fg #F8FAFC, muted #94A3B8, accent/positive #22C55E, amber #F59E0B, red #EF4444. Fonts: Fira Sans (UI) + Fira Code (all numbers, tabular-nums). Keep this consistent for future helio UI work. helio-dashboard app.py v2 added: ENTITY SWITCHER (pill tabs, ?entity= query param; "Group"=consolidated/all, else filter scorecards+KR+risk by entity via KR→Objective→Entity), a 🔮 Leading-indicators forecast panel (reads Daily Metrics Log, MTD avg = projected EOM vs target), restyled chat. Verified render (Group + Ch-Erawan filter). Still internal-only — run -p 8899 to view (http://100.120.167.89:8899 tailscale; / and /chat).

- **FORECAST LOOP + PREDICTIVE ALERT COMPLETE (2026-06-18):** n8n workflow `YNncktcs1LHVcU4U` "Helio — Leading Forecast Loop" (published, daily 07:30 cron `30 7 * * *`). Flow: scheduleTrigger → Notion Get KRs → Notion Get Metric Log (executeOnce) → Code "Forecast" (group readings by KR id for current YYYY-MM, run-rate proj = mean(MTD), patt = proj/target, miss = patt<0.9) → Notion "Write Forecast" (Forecast EOM + Projected % onto each Leading KR) → branch: Code "Misses only" (filter miss) → HTTP "CrewAI forecast action" (crewai variance_deep_dive w/ actual=forecast, predictive framing, retryOnFail 3) → Notion "Create predictive task" (Task Hub, Source=Forecast, "🔮 คาดว่าจะพลาด: <name> (proj N%)", CrewAI advice in Investigation). VERIFIED end-to-end: forced นัดหมาย to project 82% → predictive task created with real CrewAI corrective ("คัดกรองคุณภาพ Lead / Lead Source Analysis"). Built via MCP create_workflow_from_code (SDK) + update_workflow addNode/addConnection ops. LESSON: must publish_workflow before execute_workflow ("no active version"); Notion percent property stores fraction (0.82→82%); hermes NL-parse is flaky on very terse single-metric msgs (wrote low test reading direct via API instead). The leading/predictive layer (the user's core vision) is now COMPLETE: chat ingest → Metric Log → forecast loop → predictive alert + dashboard forecast panel. TEST ARTIFACTS to clean: นัดหมาย low reading "55 (low test)" in Metric Log + the predictive test task + earlier MRR test KR/blank KR/dup MRR tasks.

- **File import ingestion built (2026-06-18):** helio-dashboard added /import page (drag-drop xlsx/csv → /api/import/preview → Hermes maps grid to records {name,kind:reading/target/actual,value,date} matched to KR list → preview table → /api/import/commit writes: reading→Metric Log, target/actual→KR Target/Actual). openpyxl added to requirements. Preview→confirm gate makes AI-mapping safe. VERIFIED preview: CSV (นัดหมาย 86, Retention 52) → correct KR match + kind=reading. Multi-source ingestion strategy (user steer): API is just 1 of many sources; channels = Chat(done) + File(done) + API/LINE/Email(later), all landing in same Hermes-map→Notion core. Note: simple metric/value/date or metric/target/actual formats map cleanly; complex wide layouts (MPR) rely on Hermes + preview review. Still internal-only (run -p 8899). Gap analysis vs full blueprint done 2026-06-18: ~40% — DONE: n8n/hermes/crewai/EPM cascade/variance+CrewAI/LINE reminder/dashboard+launcher/leading-forecast/chat+file ingest. MISSING: pgvector RAG + doc ingestion (Module 2), Strategic War Room+Approve&Execute (Module 3), Peak connector, RBAC/auth, Observability module (5), notification-threshold config UI, Slack, LLM cost-control/PII guardrails, Next.js/shadcn frontend (currently Flask).

- File import now supports PDF too (2026-06-18): parse_upload handles .xlsx/.csv/.tsv/.pdf; PDF via pdfplumber (extract_tables + text fallback, cap 15 pages/80 rows). VERIFIED: Thai table PDF → pdfplumber → Hermes map → correct KR reading records. requirements: openpyxl + pdfplumber. Same preview→confirm→Notion pipeline for all formats.

- **RAG / Module 2 DONE (2026-07-04):** pgvector container `helio-pg` (pgvector/pgvector:pg16) on ds3 tunnel-net, internal-only, bind-mount /volume1/docker/helio-pg/data, db=helio user=helio (password in dashboard .env PG_DSN only — NOT in code/git; app.py default is passwordless). Table `knowledge`(entity,category,period,source,chunk_text,embedding vector(768)) + hnsw cosine idx. Embeddings = **Gemini `gemini-embedding-001` outputDimensionality=768** via GOOGLE_API_KEY (the AQ.Ab8 key works for generativelanguage API; text-embedding-004 NOT available on this key, gemini-embedding-001 is). dashboard /knowledge page: upload PDF/Word(.docx)/Excel/txt + metadata tags (entity/category/period) → extract_doc_text → chunk(1400/150) → embed → pgvector; /api/knowledge/{ingest,list,search}; search = cosine, entity-filterable. VERIFIED: ingested Thai policy.txt → semantic search "รับประกันกี่ปี" returned right chunk score 0.762; data persists across recreate. deps added: psycopg2-binary, python-docx. 6 ds3 containers now: n8n3, hermes, crewai, helio-dashboard, helio-pg, cloudflare-tunnel. Committed to private github nuntnet/helio. NEXT for RAG: Strategic War Room (Module 3) = chat over this KB (retrieve→Hermes answer→Strategy→WBS→Approve&Execute→Notion+LINE). GUARD lesson: auto-mode blocks git commit if a live secret is hardcoded as a default in tracked code — keep secrets in gitignored .env only.

- **War Room (Module 3) + unified nav + Health (Module 5 stub) DONE (2026-07-04):** /warroom = RAG-augmented strategic chat: rag_retrieve (pgvector cosine, entity-filtered top-5) -> hermes_chat answers in Thai WITH [source] citations -> "แตกเป็นงาน" button -> /api/warroom/wbs (Hermes -> JSON task list) -> "Approve & Execute" -> /api/warroom/execute creates Notion Task Hub pages (Source="War Room", human-in-the-loop). VERIFIED: retention question -> retrieved policy.txt -> grounded Thai answer citing [policy.txt] + actions. Added shared nav() across all pages (Dashboard / War Room / Knowledge / Import / Quick(chat) / Health). /health = Module 5 observability stub (pings Notion/Hermes/CrewAI/pgvector/Gemini-embed, green/UP-red/DOWN, 30s refresh) — all UP verified. Committed to private github. dashboard pages now: / (workspace launcher) · /?entity= (EPM tower Module 1) · /warroom (Module 3) · /knowledge (Module 2 RAG) · /import (file ingest) · /chat (quick metric input) · /health (Module 5). REMAINING UI gap for "complete": Module 4 Target & Rules Config (OKR/budget form + notification-threshold settings) — targets currently set via chat/import/API not a dedicated form; and RBAC/auth (no login yet — anyone with the URL sees all entities). Note: every dashboard code deploy needs recreate which strips -p; user must re-run -p 8899 each time (cloudflare tunnel would fix this permanently).

- **UI workspace-scoped (2026-07-04):** entity flows via ?entity= through nav() + wsbar(); module pages (warroom/knowledge/import/chat/health) read request.args entity and scope to it; removed cross-workspace dropdowns from /knowledge + /warroom (const ENTITY from URL); /knowledge + /warroom redirect(302) to launcher if no ?entity; /api/knowledge/list filters by entity. Enter workspace via launcher card → /?entity=X → all modules scoped, nav preserves entity. FIX for the -p friction: run dashboard with bind-mount + gunicorn --reload so code deploys (just tar app.py to /volume1/docker/helio-dashboard/app.py) auto-reload WITHOUT recreate (keeps -p): `docker run ... -p 8899:8080 -v /volume1/docker/helio-dashboard/app.py:/app/app.py --env-file .../.env helio-dashboard:latest gunicorn -b 0.0.0.0:8080 -w 2 --timeout 120 --reload app:app` (rebuild+recreate still needed only when requirements change).
