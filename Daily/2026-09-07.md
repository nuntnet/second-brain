# 2026-09-07 — Monday

> Auto-generated end-of-day summary by Hermes

---

## Projects (PARA)

### Sellsuki — oc2plus CRM / Point System (OC-4348 + OC-4362 epic)

**ทำวันนี้:**
- ✅ เริ่ม OC-4348 `/feature` (01:22) — non-LIFF OTP login flow สำหรับ point claim (กรณีลูกค้าไม่ผ่าน LINE LIFF)
- ✅ ค้นพบและแก้ bug: success screen พังหลัง submit receipt
- ✅ แก้ bug: admin แก้ยอดแล้วกด approve มันเด้งกลับเป็นยอดเดิม (validated 06:04)
- ✅ ออกแบบ + implement §C **image-hash dedup** (award_dedup_registry) — กัน receipt ซ้ำ/โกง
- ✅ ออกแบบ + implement §B **read_items** — OCR extract line items จาก receipt
- ✅ อัปเดต Jira cards เดิม (ไม่สร้างใหม่) รวม 3 ประเด็น: §A OCR cross-check, §B read_items, §C image-hash
- ✅ แก้ OCR VLM config: เคย point ไป OpenRouter → เปลี่ยนเป็น **native Gemini (Google AI Studio)**
- ✅ พิสูจน์ OCR อ่าน item จากรูปจริงสำเร็จ (13:15) ด้วย Gemini 2.5 Flash
- ✅ เขียน 4 Jira cards ใต้ epic OC-2743 สำหรับ per-brand auto-match:
  - **OC-4480** [Spike] ล็อก 5 decisions (blocks 3 ใบถัดไป)
  - **OC-4481** auto-match OCR items ↔ PIS SKUs + pre-tick ใน review UI
  - **OC-4482** member submit — ยอดเป็น optional/derived
  - **OC-4483** customer result — per-item ได้แต้ม/ไม่ได้ + เหตุผล
- ✅ Landing (14:07–14:13) — push 3 branches → open/update MRs:
  - **member-api !98**: OC-4407 marketplace + OC-4348 OTP login + §C image-hash + §B read_items (migrations 009/010/011)
  - **member FE !35**: my-claims + DUPLICATE_IMAGE + success-screen fix + OTP login page
  - **backoffice FE !572 (ใหม่)**: admin approve/edit + OCR-items review UI
- ⏸️ backoffice-api backend **รอ MR !538 merge ก่อน** (no clean base — develop มี OCR providers แต่ !538 ยังไม่ merge approve base-rate)

---

### Sellsuki — Philosophy / AI Workflow Discussion

**คุยวันนี้ (session backend):**
- 🤔 Nuntawit แชร์แนวคิด "oracle workflow" — ออกแบบให้ AI ลืมทุกวัน เก็บแค่ของที่ใช้จริง
- Nuntawit ใช้ **Claude เป็น lead + Codex ผ่าน omx เป็น worker**
- `omx` = tool เรียก Codex (ยังไม่มีบนเครื่องนี้)
- ยืนยัน: ใช้ **Gemini (Google AI Studio)** ไม่ใช่ OpenRouter

---

## Key Decisions

| Decision | Detail |
|----------|--------|
| OC-4348 per-brand scope | per-brand = สินค้าที่ลงทะเบียนใน PIS หรือ admin ตรวจด้วยตาจากรูป เท่านั้น |
| auto-match policy | pre-tick เท่านั้น — admin ตัดสินใจสุดท้ายเสมอ (ไม่ auto-approve) |
| OCR provider | **Native Gemini** (google.generativeai endpoint) + `gemini-2.5-flash` — ไม่ใช้ OpenRouter prefix |
| Jira card policy | อัปเดตการ์ดเดิมแทนสร้างใหม่ (ประหยัด noise ใน backlog) |
| backoffice-api landing | รอ MR !538 (approve base-rate) merge เข้า develop ก่อน — ห้ามเปิด MR รก |

---

## Things Learned

- **Native Gemini endpoint** — Google AI Studio key ใช้กับ base_url native Gemini ได้ตรง ไม่ต้องผ่าน OpenRouter; model = `gemini-2.5-flash` (ตัด prefix `google/` ออก)
- **Gemini OCR พิสูจน์แล้ว** — extract line items จากรูปใบเสร็จจริงได้ (5 items ถูกต้อง, format JSON)
- **GitLab auto-MR trap** — push branch ใหม่จะ auto-create MR ไปที่ `main` ตาม repo template → ต้อง close ทันที, target ที่ถูกต้องคือ `develop`
- **`pkill -f tmp/main` ฆ่าทุก Go service** — member-api + consent + backoffice-api ตายหมด ต้อง restart ทุกตัว
- **file-service ต้องการ `GRPC_PORT=50057`** — ถ้าไม่ใส่ env var จะ start ไม่ขึ้น; ใช้ Procfile เป็น reference
- **image-hash dedup** — ใช้ `award_dedup_registry` table เก็บ hash รูป → ป้องกัน submit ซ้ำ
- **OCR §B read_items** — API extract items จาก VLM แล้วเก็บใน DB ให้ admin ตรวจสอบทีหลัง

---

## Open Tasks (Tomorrow / Next)

### oc2plus Point System
- [ ] **backoffice-api backend** — รอ MR !538 (feat/oc4362-approve-base-rate) merge เข้า develop แล้ว rebase + เปิด MR
- [ ] **OC-4480 [Spike]** — ล็อก 5 decisions ก่อน: brand-source, match method+threshold, ช่องยอดลูกค้า, OCR reliability bar, award engine (blocks 3 cards)
- [ ] **OC-4421** — product scope (dependency ของ OC-4481)
- [ ] **OC-4481** — auto-match OCR items ↔ PIS/resolved_skus (pending OC-4480 + OC-4421)
- [ ] **OC-4482/4483** — member submit + customer result UI (pending OC-4481)
- [ ] Review + merge MR !98, !35, !572 กับ reviewer

### AI Chat Admin (carry-forward)
- [ ] **SRE-1685** — รอ SRE provision S3/CloudFront staging (blocker ของ 13+ issues)
- [ ] **MR !21** — รอ review + merge
- [ ] **KB adapters** — รอ auth plane resolve

### ch-erawan-next (carry-forward)
- [ ] **Cars Admin CRUD** — ยังไม่ได้เริ่ม
- [ ] ตั้ง `BETTER_AUTH_URL` ใน Vercel env variables

---

## Session Stats

| Session | Project | สรุป |
|---------|---------|------|
| sellsuki-mono main | oc2plus OC-4348 + OC-4362 epic | งาน 01:22–14:13 (13 ชั่วโมง) |
| sellsuki-mono-backend | AI/workflow philosophy chat | สนทนาสั้น (03:25–05:31) |
| hermes (cron) | daily brief + end-of-day | scheduled jobs |

OCR pipeline ทำงานได้จริงแล้ว (Gemini 2.5 Flash อ่าน items ออก). Blocker เดียวคือ backoffice-api backend รอ !538.



---

## 🌙 21:00 Evening Check (Hermes cron)

No new sessions detected after 14:13 — session store shows latest activity was landing at 14:13.

**Day summary confirmed complete.** Blocker standing: backoffice-api backend awaiting MR !538 merge.

